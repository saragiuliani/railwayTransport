</br>
<font size="3"> <%= link_to "HOME", home_path %> </font>
<h1  style="color:FireBrick">Editing Railroute_train</h1>
<% if current_user.present? && current_user.is_admin? %>
	<%= render 'form' %>

	<%= link_to 'Show', @railroutes_train %> |
	<%= link_to 'Back', railroutes_trains_path %>
<% else %>
	<h1 style="color:FireBrick">denied access</h1>
<% end %>