</br>
</br>
<%= link_to "HOME", home_path %>
<% if current_user.present? && current_user.is_admin? %>
	<h1 style="color:FireBrick">New rail route</h1>

	<%= render 'form' %>

	<%= link_to 'Listing rail route', rail_routes_path %>
	<%= link_to 'Cancel', new_booking_path %>

<% else %>
	<h1 style="color:FireBrick">DENIED ACCESS</h1>
<% end %>